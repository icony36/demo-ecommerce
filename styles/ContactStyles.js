export default theme => ({
    container: {
        textAlign: "left"
    },
    title: {
        fontSize: "3em",
        marginTop: theme.spacing(6),
        fontWeight: 500
    },
    emailInput: {
        margin: theme.spacing(2)
    }
});
